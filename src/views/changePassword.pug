extends layouts/main.pug

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="password", name="oldPassword", placeholder="현재 비밀번호")
            input(type="password", name="newPassword", placeholder="새로운 비밀번호")
            input(type="password", name="newPassword1", placeholder="비밀번호 다시 입력")
            input(type="submit", value="저장")